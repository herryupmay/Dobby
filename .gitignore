# Build artifacts (regenerated for each release)
*.zip

# Chrome Web Store listing assets (kept out of source tree)
icons/Screenshot*

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Local env / secrets (none today, but a guard for later)
.env
.env.local
